export const spacing = (units: number) => Math.round(units * 8);

export const layout = {
  gutter: 24,
  element: 12,
  section: 16,
  major: 24,
  contentPadding: 20,
  compact: 8,
  touchTarget: 44,
};
